specVersion: "v1"
serviceId: "propagentic"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "propagentic-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
